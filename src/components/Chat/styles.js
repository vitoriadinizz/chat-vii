// Importa a função "styled" da biblioteca "styled-components"
import styled from "styled-components";

// Define um componente estilizado chamado "Container" usando a função "styled"
export const Container = styled.div`
  // Define o estilo do componente
  display: flex;         // Estabelece um display flex para o elemento
  flex-direction: column; // Define a direção de fluxo como uma coluna
  width: 100%;           // Define a largura do elemento como 100%
`;
